name = "earnandclaimcoins"
compatibility_date = "2024-01-01"

[site]
bucket = "./site/dist"

[pages]
# Root directory is the repo root (default).
# Build runs inside /site, outputs to /site/dist.
build_command = "cd site && npm install && npm run build"
build_output_dir = "site/dist"
